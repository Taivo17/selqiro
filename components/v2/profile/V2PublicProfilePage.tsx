import PublicProfilePage from "../../../src/features/public-profile/components/PublicProfilePage";

export default function V2PublicProfilePage({ slug }: { slug: string }) {
  return <PublicProfilePage slug={slug} />;
}
