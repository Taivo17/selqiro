# Identity API

This folder will contain identity-related data access.

Planned functions:

- getActiveIdentity
- getMyIdentities
- setActiveIdentity
- getActiveIdentityProfile
- getIdentityPublicProfile

First implementation should be small:

getActiveIdentity should read the existing active identity from current database logic and map it into IdentitySummary.
