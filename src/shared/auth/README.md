# Auth shared layer

This folder contains authentication helpers for V2 production code.

Launch rule:

Reuse existing auth behavior where it is stable.

Do not scatter auth/session checks throughout large UI components.
