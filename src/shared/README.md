# Shared

Shared contains reusable infrastructure and utilities.

Examples:

- Supabase client access
- auth helpers
- UI primitives later
- formatting helpers
- i18n helpers
- config helpers

Shared code should not contain feature-specific business logic.
